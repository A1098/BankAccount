class BankAccount:
    BankName = "DojoOne"
    accounts=[]
    def __init__(self, int_rate, balance): 
        self.int_rate = int_rate
        self.balance = balance
        BankAccount.accounts.append(self)
    def deposit(self, amount):
        self.balance += amount
        print(f"{self} a Deposit of, {str(amount)}, was successfully processed!")
        print(f"Balance: {self.balance}")
        return self
    def withdraw(self, amount):
        if self.balance > 0:
            self.balance -= amount
            print("A withdraw of", amount, "was successfully processed")
            print("Your acount balance is", self.balance)
        else:
            self.balance -= 5
            print("Insufficient funds: Charging a $5 fee")
        return self
    def yield_interest(self):
        if self.balance > 0:
            int_pymt = self.int_rate * self.balance
            self.balance += int_pymt
            print(f"Your interest payment today will be {int_pymt} ")
            print(f"{self} has succesfully received interest payment")
            print("account balance:", self.balance)
            return self
    def display_account_info(self):
        print(f"Balance: {self.balance}")
        return self
    @classmethod
    def all_accounts(cls):
        return cls.accounts

Madrid = BankAccount(.05, 600)
Ferrencini = BankAccount(.03, 400)
Madrid.deposit(50).deposit(200).deposit(200).withdraw(200).yield_interest().display_account_info
Ferrencini.deposit(200).deposit(50).withdraw(20).withdraw(20).withdraw(40).withdraw(20).yield_interest().display_account_info
print(BankAccount.all_accounts())
















































































































































































































































































































































































































































































































































































































































































































































